1.25 - 2.8 目标：
    1：完成一个通用的segmenter与matrix processor（处理分类问题）
    2：能够完成一些用于文本分类的算法

1.25 目标：
    1：完成一个通用到segmenter，并完成测试

1.26-1.27 目标：
    思考一下用户场景，进行一下简单的设计，写出一个示例程序
    完成了一个naive-bayes的分类器，现在需要能够在分类中获取更多的结果

1.28 目标：
    重构整个项目，能够随时拿到需要的信息，以供分析
    下面的信息是有需要的：(这些信息需要在处理成矩阵的时候就生成）
    1）term -> id
    2）id -> term
    3）term -> doc-count
    4）class -> doc-count
    对于矩阵中vals数组，也尽快能够在nb中用上
